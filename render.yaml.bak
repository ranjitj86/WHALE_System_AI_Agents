services:
  - type: web
    name: whale-requirements-engineering
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd AutoTestGen_MAPS && python app.py
    envVars:
      - key: SECRET_KEY
        generateValue: true 